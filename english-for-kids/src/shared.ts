// import { getCategoriesCardsInfo } from "./api/api";
// import { ImageCategoryModel } from "./models/image-category-model";

// export async function showTrain(category: string) {
//   const categories = await getCategoriesCardsInfo();
//   const categoryItem: ImageCategoryModel | undefined = categories.find(cat => cat.category === category);

//   this.game.newGame(categoryData);

// }
